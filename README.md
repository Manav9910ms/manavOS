# manavOS

A cloud-first personal computer experience: your computer follows you across devices.

## MVP

The first milestone is a browser-based desktop shell with a clear path toward persistent remote compute. The UI is intentionally separated from the future VM/remote-display control plane.

## Development

```bash
npm install
npm run dev
```

Then open http://localhost:3000.
